import { Package } from "lucide-react";
import { Placeholder } from "./Placeholder";

export default function Produtos() {
  return <Placeholder title="Produtos" moduleNumber={2} icon={Package} />;
}
